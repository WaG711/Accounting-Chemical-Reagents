import 'package:flutter/material.dart';

class ReadyRecipe extends StatelessWidget {
  const ReadyRecipe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
